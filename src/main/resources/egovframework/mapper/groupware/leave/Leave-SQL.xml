<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.groupware.leave.mapper.LeaveMapper">

    <resultMap id="reqMap" type="egovframework.groupware.leave.service.LeaveRequestVO">
        <id property="leaveId" column="leave_id"/>
        <result property="userId" column="user_id"/>
        <result property="userName" column="user_name"/>
        <result property="leaveTypeCd" column="leave_type_cd"/>
        <result property="startDt" column="start_dt"/>
        <result property="endDt" column="end_dt"/>
        <result property="startAt" column="start_at"/>
        <result property="endAt" column="end_at"/>
        <result property="days" column="days"/>
        <result property="reason" column="reason"/>
        <result property="statusCd" column="status_cd"/>
        <result property="approvalDocId" column="approval_doc_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <resultMap id="balMap" type="egovframework.groupware.leave.service.LeaveBalanceVO">
        <result property="userId" column="user_id"/>
        <result property="year" column="bal_year"/>
        <result property="annualGiven" column="annual_given"/>
        <result property="annualUsed" column="annual_used"/>
    </resultMap>

    <insert id="insertRequest" parameterType="egovframework.groupware.leave.service.LeaveRequestVO"
            useGeneratedKeys="true" keyProperty="leaveId" keyColumn="leave_id">
        INSERT INTO gw_leave_req (user_id, leave_type_cd, start_dt, end_dt,
                                  start_at, end_at, days, reason,
                                  status_cd, approval_doc_id)
        VALUES (#{userId}, #{leaveTypeCd}, #{startDt}, #{endDt},
                #{startAt}, #{endAt}, #{days}, #{reason},
                COALESCE(#{statusCd}, 'IN_PROGRESS'), #{approvalDocId})
    </insert>

    <update id="updateRequestStatus">
        UPDATE gw_leave_req
           SET status_cd = #{statusCd}, approval_doc_id = COALESCE(#{approvalDocId}, approval_doc_id),
               updated_at = CURRENT_TIMESTAMP
         WHERE leave_id = #{leaveId}
    </update>

    <select id="findById" resultMap="reqMap">
        SELECT r.*, u.name AS user_name FROM gw_leave_req r
          JOIN gw_user u ON u.user_id = r.user_id WHERE r.leave_id = #{leaveId}
    </select>

    <select id="findByApprovalDoc" resultMap="reqMap">
        SELECT r.*, u.name AS user_name FROM gw_leave_req r
          JOIN gw_user u ON u.user_id = r.user_id WHERE r.approval_doc_id = #{docId}
    </select>

    <select id="listByUser" resultMap="reqMap">
        SELECT r.*, u.name AS user_name FROM gw_leave_req r
          JOIN gw_user u ON u.user_id = r.user_id
         WHERE r.user_id = #{userId}
         ORDER BY r.created_at DESC LIMIT 200
    </select>

    <select id="findBalance" resultMap="balMap">
        SELECT user_id, bal_year, annual_given, annual_used
          FROM gw_leave_balance WHERE user_id = #{userId} AND bal_year = #{year}
    </select>

    <update id="upsertBalance">
        MERGE INTO gw_leave_balance t
        USING (VALUES (#{userId}, #{year}, #{given})) AS v(user_id, bal_year, annual_given)
           ON t.user_id = v.user_id AND t.bal_year = v.bal_year
        WHEN MATCHED THEN
            UPDATE SET annual_given = v.annual_given
        WHEN NOT MATCHED THEN
            INSERT (user_id, bal_year, annual_given, annual_used)
            VALUES (v.user_id, v.bal_year, v.annual_given, 0)
    </update>

    <update id="addUsed">
        UPDATE gw_leave_balance
           SET annual_used = annual_used + #{days}, updated_at = CURRENT_TIMESTAMP
         WHERE user_id = #{userId} AND bal_year = #{year}
    </update>

    <resultMap id="balRowMap" type="egovframework.groupware.leave.service.LeaveBalanceRow">
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="deptName" column="dept_name"/>
        <result property="hireDate" column="hire_date"/>
        <result property="year" column="bal_year"/>
        <result property="annualGiven" column="annual_given"/>
        <result property="annualUsed" column="annual_used"/>
    </resultMap>

    <select id="listBalancesForYear" resultMap="balRowMap">
        SELECT u.user_id, u.name, u.email, u.hire_date,
               d.dept_nm AS dept_name,
               #{year}   AS bal_year,
               COALESCE(b.annual_given, 0) AS annual_given,
               COALESCE(b.annual_used,  0) AS annual_used
          FROM gw_user u
          LEFT JOIN gw_dept d ON d.dept_id = u.dept_id
          LEFT JOIN gw_leave_balance b
                 ON b.user_id = u.user_id AND b.bal_year = #{year}
         WHERE u.deleted_at IS NULL AND u.use_yn = 'Y'
        <if test="keyword != null and keyword != ''">
           AND (u.name  ILIKE '%' || #{keyword} || '%'
             OR u.email ILIKE '%' || #{keyword} || '%'
             OR d.dept_nm ILIKE '%' || #{keyword} || '%')
        </if>
         ORDER BY d.dept_nm NULLS LAST, u.name
    </select>
</mapper>
